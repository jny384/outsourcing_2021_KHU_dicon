import React from "react";
// import storage from "../firebase/firebase-manager";

export default function GetWorkImages(props) {
    // const directory = props.directory;
    console.log(props.url);
    // const pathRef = storage.ref(`images/${directory}/works`)
    return(
        <>
            {/*{imgUrls.map((url, i) => (*/}
            {/*    <img key={i}*/}
            {/*         src={url || "http://via.placeholde.com/300"}*/}
            {/*         alt="firebase-image"*/}
            {/*         style={{width: "100px"}}*/}
            {/*    />*/}
            {/*))}*/}
        </>
    )
}