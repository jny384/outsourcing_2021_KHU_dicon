import React from "react";
// import storage from "../firebase/firebase-manager";

export default function GetProfileImage(url) {
    // const directory = props.directory;
    // const profileUrl = url;
    console.log(url)

    // const pathRef = storage.ref(`gs://bucket/images/${directory}/profile`)

    return(
        <>
            {/*<img*/}
            {/*    src={url || "http://via.placeholde.com/300"}*/}
            {/*    alt="firebase-image"*/}
            {/*    style={{width: "100px"}}*/}
            {/*/>*/}
        </>
    )

}